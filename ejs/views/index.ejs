<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dynamic EJS Example</title>
</head>
<body>
    <h1>Welcome to EJS Dynamic HTML</h1>

    <h2>Users:</h2>
    <ul>
        <% users.forEach(user => { %>
            <li><strong><%= user.name %></strong> (Age: <%= user.age %>)</li>
        <% }); %>
    </ul>

    <h2>Posts:</h2>
    <ul>
        <% posts.forEach(post => { %>
            <li><strong><%= post.title %></strong> by <%= post.author %></li>
        <% }); %>
    </ul>
</body>
</html>
